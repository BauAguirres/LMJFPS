---
const { preguntas } = Astro.props;
---
<section class="my-8 md:my-16">
  <div class="mx-auto max-w-6xl px-4 md:px-6">
    <div class="flex items-center gap-4 mb-6 md:mb-12">
      <div class="h-8 w-1.5 bg-yellow-500"></div>
      <h1 class="text-2xl md:text-4xl font-black uppercase tracking-wide text-white">Preguntas Frecuentes</h1>
    </div>

    <div class="space-y-3 md:space-y-4">
      {preguntas?.map((item: any) => (
        <details class="border border-red-500/30 bg-zinc-900 p-3 md:p-4 rounded">
          <summary class="cursor-pointer font-bold text-sm md:text-base text-white uppercase tracking-wide">
            {item.pregunta}
          </summary>
          <p class="mt-3 md:mt-4 text-zinc-300 text-xs md:text-sm leading-relaxed">{item.respuesta}</p>
        </details>
      ))}
    </div>
  </div>
</section>